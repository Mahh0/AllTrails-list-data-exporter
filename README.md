# AllTrails-list-data-exporter
Extraire les randonnées d'une liste alltrails

Script js à copier dans le navigateur dans F12 > Console, en étant sur une liste (page accessible via partager, copier le lien, pas celle avec la map par défaut)
